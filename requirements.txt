# Core
torch>=2.2
torchvision>=0.17
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0

# ONNX / quantization deployment path
onnx>=1.15
onnxruntime>=1.17

# Deployment
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.32
python-multipart>=0.0.9
pillow>=10.0

# Dev / testing
pytest>=8.0
pytest-cov>=4.1

# Note: this file installs the default (often CUDA-enabled) PyPI wheel.
# For a CPU-only install (recommended for the deployment container and for
# any machine without a GPU), instead run:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# then: pip install -r requirements.txt --no-deps -r requirements-nontorch.txt
# (see README "Installation" section for the two-step CPU install).
